// ─── WhiteboardModule (TurboModule) ───────────────────────────────────────────
// ObjC header. The generated spec protocol (NativeWhiteboardModuleSpec) is
// produced by codegen and lives in the build directory — it won't exist until
// you run `pod install` or `yarn react-native codegen`.

#import <Foundation/Foundation.h>
#import <React/RCTBridgeModule.h>

// The generated spec header will be at (after codegen runs):
// ios/build/generated/ios/ReactCodegen/AppSpecsJSI.h
// That header declares NativeWhiteboardModuleSpecJSI which this class uses.

@interface WhiteboardModule : NSObject <RCTBridgeModule>
@end
